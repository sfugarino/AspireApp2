---
apiVersion: "v1"
kind: "Secret"
metadata:
  name: "mongo-secrets"
  labels:
    app.kubernetes.io/name: "aspireapp2-apphost"
    app.kubernetes.io/component: "mongo"
    app.kubernetes.io/instance: "{{.Release.Name}}"
stringData:
  MONGO_INITDB_ROOT_PASSWORD: "{{ .Values.secrets.mongo.MONGO_INITDB_ROOT_PASSWORD }}"
type: "Opaque"
